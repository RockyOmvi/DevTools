{
  "name": "dev-tools-hub",
  "version": "1.0.0",
  "description": "Multi-page Developer Tools Hub",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "npx -y http-server ./",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
